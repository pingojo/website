name: Test Website Field

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  test-website-field:
    runs-on: ubuntu-latest

    steps:
      # Checkout the repository
      - name: Checkout Code
        uses: actions/checkout@v3

      # Set up Node.js environment
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '16'

      # Run the test script
      - name: Run Website Field Test
        run: |
          # Create a simple test script
          echo "const { createClient } = require('@supabase/supabase-js'); \
          
          (async () => { \
            const supabaseUrl = 'https://eyghpgfznmuahcqohmkk.supabase.co'; \
            const supabaseKey = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImV5Z2hwZ2Z6bm11YWhjcW9obWtrIiwicm9sZSI6ImFub24iLCJpYXQiOjE2OTM5NTY4MDMsImV4cCI6MjAwOTUzMjgwM30.j_5poF_GPEZ6RoMwNjz1ndxaaw6CdGDFLuVVbJR_MAs'; \
            const supabase = createClient(supabaseUrl, supabaseKey); \
            const { data, error } = await supabase.from('company').select('*').limit(1).single(); \
            
            if (error) { \
              throw new Error('Failed to fetch data: ' + error.message); \
            } \
            
            console.log('Fetched data:', data); \
            
            if ('website' in data) { \
              throw new Error('Error: The "website" field exists in the data.'); \
            } \
            
            console.log('Test Passed: No "website" field found.'); \
          })();" > test-website-field.js

          # Run the test
          node test-website-field.js